## 2026-05-04

- Вынесены стили из `index.html` в `styles/main.css`.
- Вынесен JavaScript из `index.html` в `scripts/main.js`.
- В `index.html` подключен модульный скрипт: `type="module"` для `scripts/main.js`.
- Создан файл `scripts/utils.js`.
- Функция `wrapLetters` вынесена в `scripts/utils.js` и импортируется в `scripts/main.js`.

## 2026-05-05

- Доска переработана в paint-режим с инструментами: кисть, ластик, линия, прямоугольник, круг, заливка, текст, `Текст + рамка`, `Умная рамка`.
- Добавлены роли и доступы: мастер редактирует только свою ББ-доску, начальник просматривает все доски в режиме read-only.
- Реализованы сохранение/загрузка досок по ББ через `localStorage`, история действий (`undo`) и экспорт в PNG.
- Улучшен текстовый редактор на доске: выбор шрифта/размера/стилей, применение стилей к выделению, сохранение форматирования в canvas.
- Исправлены проблемы с сохранением текста при клике вне рамки, предотвращен повторный старт отрисовки в тот же клик.
- Стабилизировано соответствие визуала «до/после сохранения» за счет сериализации `computed styles` в SVG `foreignObject`.
- Починен выбор цвета текста и синхронизировано превью цветовой кнопки с текущим цветом.
- Обновлен UI тулбара: ползунки ширины/размера доведены до края, добавлен расширенный список шрифтов, включая рукописные.
