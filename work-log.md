## 2026-05-04

- Вынесены стили из `index.html` в `styles/main.css`.
- Вынесен JavaScript из `index.html` в `scripts/main.js`.
- В `index.html` подключен модульный скрипт: `type="module"` для `scripts/main.js`.
- Создан файл `scripts/utils.js`.
- Функция `wrapLetters` вынесена в `scripts/utils.js` и импортируется в `scripts/main.js`.

## 2026-05-05

- Доска переработана в paint-режим с инструментами: кисть, ластик, линия, прямоугольник, круг, заливка, текст, `Текст + рамка`, `Умная рамка`.
- Добавлены роли и доступы: мастер редактирует только свою ББ-доску, начальник просматривает все доски в режиме read-only.
- Реализованы сохранение/загрузка досок по ББ через `localStorage`, история действий (`undo`) и экспорт в PNG.
- Улучшен текстовый редактор на доске: выбор шрифта/размера/стилей, применение стилей к выделению, сохранение форматирования в canvas.
- Исправлены проблемы с сохранением текста при клике вне рамки, предотвращен повторный старт отрисовки в тот же клик.
- Стабилизировано соответствие визуала «до/после сохранения» за счет сериализации `computed styles` в SVG `foreignObject`.
- Починен выбор цвета текста и синхронизировано превью цветовой кнопки с текущим цветом.
- Обновлен UI тулбара: ползунки ширины/размера доведены до края, добавлен расширенный список шрифтов, включая рукописные.

## 2026-05-05 (вечер)

- Создан `scripts/auth.js`: авторизация вынесена из `scripts/main.js` в отдельный файл.
- Создан `scripts/storage.js`: сохранение и загрузка доски вынесены из `scripts/main.js`.
- В `scripts/main.js` обновлены вызовы на новые функции:
  - `authenticateUser(...)`
  - `saveBoardToStorage(...)`
  - `loadBoardFromStorage(...)`
- Добавлен `manual-test-checklist.md` для быстрой ручной проверки после правок.
- В `scripts/main.js` добавлены русские комментарии-разделы для навигации по коду.

## 2026-05-07

- Полностью удален legacy paint-редактор; доска переведена на `Fabric` (`test.html`) как единственный рабочий режим.
- Интегрирован `Fabric` в основной поток приложения через `iframe` и навигацию по ББ с учетом ролей.
- Для мастера добавлена страница шаблонов досок: создание новой доски (`+`), открытие, предпросмотр, удаление шаблонов.
- В шаблонах реализованы миниатюры и увеличенный предпросмотр; добавлены hover-действия `Открыть`, `Предпросмотр`, `Удалить`.
- Добавлены закладки досок и отдельная кнопка `Добавить в закладки` в тулбаре редактора.
- Кнопка `Сохранить и выйти` перенесена в тулбар Fabric; перед выходом поддержан выбор области для сохранения превью.
- Реализован режим выбора области сохранения (с отменой по `Esc`) и точное сохранение выбранного фрагмента в превью.
- Существенно улучшено качество preview/thumbnail: раздельные источники, повышенный multiplier, точное кадрирование.
- Добавлены UX-улучшения редактора: `Отменить`, `Ctrl+Z/Cmd+Z`, скрытие/показ панели слоев, отключение guide-lines.
- Добавлен полноэкранный режим для демонстраций: скрытие тулбара/слоев и плавающая кнопка выхода в обычный режим.
- Улучшены контролы объектов (стрелки ресайза со всех сторон/углов, иконка поворота, точка перемещения), а также стабильность ввода текста.

## 2026-05-07 (аудит состояния)

- Проект находится в стадии рабочего UI-прототипа: ключевой сценарий `вход -> выбор ББ/шаблона -> редактирование -> сохранение/выход` работает в фронтенде.
- Основная логика маршрута и ролей сосредоточена в `scripts/main.js`; редактор и инструменты — в `test.html` (Fabric + тулбар + слои + экспорт/импорт).
- Сильные стороны: полный пользовательский поток, роли (мастер/начальник), предпросмотры шаблонов, fullscreen-режим и улучшенный UX редактора.
- Технические риски: клиентская авторизация (`scripts/auth.js`), хранение только в `localStorage`, большой объем inline-логики в `test.html`, необходимость строгой проверки `postMessage`.
- Приоритет следующих шагов: минимальный backend для auth/storage, модульная декомпозиция `test.html`, smoke-регресс по `manual-test-checklist.md` после каждого pull, синхронизация документации с актуальным кодом.
